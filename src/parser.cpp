#include <iostream>
#include <stdio.h>
#include <vector>
#include "parser.h"

std::vector<std::string> parseString(std::string str) {
    std::cout << str << "\n";
    std::vector<std::string> res;

    return res;
}
